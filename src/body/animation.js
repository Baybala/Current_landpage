// function animate (ref1, ref2) {
// function reveal() {
//       const bodyElementTop = ref.current.getBoundingClientRect().top
//       const chooseUsElementTop = chooseUsRef.current.getBoundingClientRect().top
//       const screenHeight = window.screen.height

//       if (bodyElementTop < screenHeight) {
//         setBodyClassName('advantageWindowActive')
//       }

//       if (chooseUsElementTop + 100 < screenHeight) {
//         setChooseUsRefClassName('chooseUSActive')
//       }
//     }

//     window.addEventListener('scroll', reveal)
// }
